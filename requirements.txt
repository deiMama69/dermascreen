# Kern
tensorflow==2.17.1          # Wheels für aktuelles Colab-Python (3.12) UND lokal 3.11
tf-keras==2.17.0            # reaktiviert Keras-2-Verhalten (via TF_USE_LEGACY_KERAS=1)
numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.3
pyyaml>=6.0
matplotlib>=3.7
tqdm>=4.66
requests>=2.31
Pillow>=10.0

# Optional: offizielles ISIC-CLI (alternativer Download-Weg)
# isic-cli>=6.0
