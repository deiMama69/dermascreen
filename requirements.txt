# Kern — an Colabs numpy-2-Welt angepasst (KEIN numpy-Downgrade mehr!)
tensorflow==2.20.0          # entspricht Colabs Standard-TF; laeuft auch lokal (Python 3.11-3.13)
tf-keras==2.20.0            # reaktiviert Keras-2-Verhalten (via TF_USE_LEGACY_KERAS=1)
numpy>=2.0,<3
pandas>=2.0
scikit-learn>=1.3
pyyaml>=6.0
matplotlib>=3.7
tqdm>=4.66
requests>=2.31
Pillow>=10.0

# Optional: offizielles ISIC-CLI (alternativer Download-Weg)
# isic-cli>=6.0
